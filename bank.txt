git clone(): add exact copy of remote respository to local respository 

git status(): to get the list of untracked files in git

git add -A : to allow the exact copy of the created file in the git folder to staging process

git commit -m "message" : to commit the changes to the files in git

git push origin main : to push the files to the remote respository from the local respository